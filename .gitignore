# Secrets and local env
.env
.env.local
*.pem
*.key

# Project-internal planning docs (not for the public repo)
CLAUDE.md
plan.md

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Vercel
.vercel
# Auto-generated locally by `vercel build`'s Python (uv) tooling; requirements.txt is the source of truth
.python-version
pyproject.toml
uv.lock

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Scratch
.scratch/
# Added by `vercel link` to cover .env.local (holds a VERCEL_OIDC_TOKEN).
# .env.example is the one env file that must stay tracked.
.env*
!.env.example
